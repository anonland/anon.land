import { FullCategoryPipe } from './full-category.pipe';

describe('FullCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FullCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
